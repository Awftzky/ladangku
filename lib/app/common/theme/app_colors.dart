library;

import 'package:flutter/material.dart';

/* NOTES : MAIN COLOR HANYA WARNA YANG FIX */
class AppColors {
  AppColors._();

  // --- Main Color ---
  static const Color primary = Color(0xFF546B41);
  static const Color secondary = Color(0xFF99AD7A);
  static const Color accent = Color(0xFFDCCCAC);
  static const Color background = Color(0xFFFFF8EC);

  // --- Theme Scaffold & Surface Colors ---
  static const Color lightBackground = Color(0xFFFFF8EC);
  static const Color darkBackground = Color(0xFF11140E);

  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color darkSurface = Color(0xFF1B1F17);

  static const Color lightOnSurface = Color(0xFF1A1C18);
  static const Color darkOnSurface = Color(0xFFE2E3DC);

  static const Color lightOutline = Color(0xFF74796E);
  static const Color darkOutline = Color(0xFF8E9286);

  static const Color primaryLight = Color(0xFFADC98B);
  static const Color primaryDark = Color(0xFF243616);

  // --- Background Color ---
  static const Color defaultBackground = Color(0xFFFFFFFF);

  // --- Indicator / Semantic Colors ---
  static const Color error = Color(0xFFE53935);
  static const Color success = Color(0xFF43A047);
  static const Color warning = Color(0xFFFB8C00);
  static const Color info = Color(0xFF1E88E5);

  static const Color white = Color(0xFFFFFFFF);
}
